#============================ 设备平台 ============================
CONFIG_TARGET_qualcommax=y                     # 选择 Qualcomm AXi 系列平台
CONFIG_TARGET_qualcommax_ipq60xx=y             # 选择 IPQ60xx 子平台

#============================ 设备列表 ============================
CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_cmiot_ax18=y   # 编译 中国移动 AX18 机型
CONFIG_TARGET_DEVICE_qualcommax_ipq60xx_DEVICE_zn_m2=y        # 编译 兆能 M2 机型

#============================ WIFI驱动 (全部已禁用) ============================
CONFIG_PACKAGE_kmod-ath=n                      # Atheros 基础无线驱动（已取消）
CONFIG_PACKAGE_kmod-ath11k=n                   # QCA ath11k WiFi6 驱动（已取消）
CONFIG_PACKAGE_kmod-ath11k-ahb=n               # ath11k AHB 接口支持（已取消）
CONFIG_PACKAGE_kmod-ath11k-pci=n               # ath11k PCIe 接口支持（已取消）
CONFIG_PACKAGE_ath11k-firmware-ipq6018=n       # IPQ6018 专用 Wi-Fi 固件（已取消）
CONFIG_PACKAGE_ath11k-firmware-ipq6018-ddwrt=n # IPQ6018 DD-WRT 版本固件（已取消）
CONFIG_PACKAGE_ath11k-firmware-qcn9074=n       # QCN9074 网卡固件（已取消）
CONFIG_PACKAGE_ath11k-firmware-qcn9074-ddwrt=n # QCN9074 DD-WRT 版固件（已取消）
CONFIG_PACKAGE_wpad-openssl=n                  # 无线认证客户端/服务端（已取消）
CONFIG_PACKAGE_hostapd-common=n                # hostapd 无线接入点组件（已取消）

#============================ 科学插件调整 ============================
#CONFIG_PACKAGE_luci-app-homeproxy=y           # 不支持xhttp  不用了
CONFIG_PACKAGE_luci-app-passwall=y             #  
#
# Configuration
#
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Haproxy=y
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Hysteria is not set
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_NaiveProxy is not set
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Client=n
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Server=n
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadow_TLS is not set
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Simple_Obfs=n
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox=n
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_tuic_client is not set
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Geodata is not set
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Geoview=n
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Plugin=n
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray_Plugin is not set
# end of Configuration

#============================ 增加luci-app插件 ============================
CONFIG_PACKAGE_luci-app-argon-config=y         # 一个主题argon的配置    					
CONFIG_PACKAGE_luci-app-arpbind=y              # arp绑定  										
CONFIG_PACKAGE_luci-app-cpufreq=y              # 调整cpu主频及策略的
CONFIG_PACKAGE_luci-app-ddns=y                 # DDNS，需要再编译上ddns-scripts_aliyun等
CONFIG_PACKAGE_luci-app-eqos=y                 # ip限速
CONFIG_PACKAGE_luci-app-filemanage=y           # 文件管理
CONFIG_PACKAGE_luci-app-frps=y                 # frp穿透服务器
CONFIG_PACKAGE_luci-app-xfrpc=y                # frp客户端 代替N2N看看
CONFIG_PACKAGE_luci-app-n2n=y                  # 内网穿透	 保留，frp代替								 
CONFIG_PACKAGE_luci-proto-wireguard=y          # VPN内网穿透，包含了wireguard-tools kmod-wireguard
																 
CONFIG_PACKAGE_luci-app-ramfree=y              # 释放内存   
CONFIG_PACKAGE_luci-app-sqm=y                  # 队列
CONFIG_PACKAGE_luci-app-timecontrol=y          # 时间控制	
CONFIG_PACKAGE_luci-app-timewol=y              # 定时网络唤醒
CONFIG_PACKAGE_luci-app-ttyd=n                 # 网页ssh
CONFIG_PACKAGE_luci-app-uhttpd=y               # uhtppd的端口啥的配置
CONFIG_PACKAGE_luci-app-unblockmusic=y         # 网易云	 不需要了，没用吧	
CONFIG_PACKAGE_luci-app-vlmcsd=y               # KMS
CONFIG_PACKAGE_luci-app-watchcat=y             # 断网检测	

CONFIG_PACKAGE_luci-app-autoreboot=y           # 定时重启任务管理界面
CONFIG_PACKAGE_luci-app-gecoosac=n             # 集客无线控制器（⚠️已取消）
CONFIG_PACKAGE_luci-app-mini-diskmanager=y     # 轻量级磁盘管理（挂载/卸载/格式化）
CONFIG_PACKAGE_luci-app-partexp=y              # 一键自动分区、格式化、扩容、挂载
CONFIG_PACKAGE_luci-app-samba4=n               # Samba4 文件共享服务（⚠️已取消）
CONFIG_PACKAGE_luci-app-upnp=y                 # UPnP 服务界面（自动端口映射）
CONFIG_PACKAGE_luci-app-wolplus=y              # 加强版网络唤醒工具

#============================ 删除luci-app插件 ============================
CONFIG_PACKAGE_luci-app-attendedsysupgrade=n   # 在线固件升级服务（已禁用）
CONFIG_PACKAGE_luci-app-wol=n                  # 原版网络唤醒工具（已禁用，改用 wolplus）

#============================ 参数调整 ============================
CONFIG_CCACHE=y                                # 启用 ccache 加速重复编译
CONFIG_DEVEL=y                                 # 包含开发辅助工具
CONFIG_FEED_video=n                            # 禁用视频类软件源（如 ffmpeg）
CONFIG_TARGET_MULTI_PROFILE=y                  # 支持多设备同时编译
CONFIG_TARGET_PER_DEVICE_ROOTFS=y              # 每个设备独立生成根文件系统
CONFIG_TARGET_ROOTFS_INITRAMFS=n               # 不生成 initramfs 内核（恢复模式用）

#============================ 内核调整 ============================
CONFIG_PACKAGE_kmod-ata-ahci=n                 # AHCI SATA 控制器驱动（⚠️已取消）
CONFIG_PACKAGE_kmod-ata-core=n                 # ATA/SATA 核心支持（⚠️已取消）
CONFIG_PACKAGE_kmod-bonding=y                  # 链路聚合（多网口绑定）
CONFIG_PACKAGE_kmod-dsa=y                      # 分布式交换架构驱动（IPQ60xx 必须）
CONFIG_PACKAGE_kmod-dummy=y                    # 虚拟 dummy 网卡（内核测试/占位）
CONFIG_PACKAGE_kmod-fs-btrfs=y                 # Btrfs 文件系统支持
CONFIG_PACKAGE_kmod-fs-ksmbd=n                 # 内核态 SMB 服务器 ksmbd（⚠️已取消）
CONFIG_PACKAGE_kmod-fuse=y                     # FUSE（用户空间文件系统）支持
CONFIG_PACKAGE_kmod-inet-diag=y                # 网络诊断接口（ss 命令依赖）
CONFIG_PACKAGE_kmod-inet-mptcp-diag=y          # MPTCP 连接诊断
CONFIG_PACKAGE_kmod-mtd-rw=y                   # 解除 MTD 分区只读限制（刷写用）
CONFIG_PACKAGE_kmod-netlink-diag=y             # Netlink 诊断（ss 命令依赖）
CONFIG_PACKAGE_kmod-nf-nat6=y                  # IPv6 NAT6 支持（校园网等环境）
CONFIG_PACKAGE_kmod-nft-arp=y                  # nftables ARP 匹配支持
CONFIG_PACKAGE_kmod-nft-bridge=y               # nftables 桥接过滤支持
CONFIG_PACKAGE_kmod-nft-connlimit=y            # nftables 连接数限制
CONFIG_PACKAGE_kmod-nft-core=y                 # nftables 核心模块
CONFIG_PACKAGE_kmod-nft-dup-inet=y             # nftables 数据包复制
CONFIG_PACKAGE_kmod-nft-fib=y                  # nftables 路由表匹配
CONFIG_PACKAGE_kmod-nft-fullcone=y             # nftables Fullcone NAT 支持
CONFIG_PACKAGE_kmod-nft-nat=y                  # nftables NAT 基础支持
CONFIG_PACKAGE_kmod-nft-netdev=y               # nftables netdev 家族支持
CONFIG_PACKAGE_kmod-nft-offload=y              # nftables 硬件流卸载支持
CONFIG_PACKAGE_kmod-nft-queue=y                # nftables 队列分发（配合用户态程序）
CONFIG_PACKAGE_kmod-nft-socket=y               # nftables socket 匹配
CONFIG_PACKAGE_kmod-nft-tproxy=y               # nftables 透明代理支持
CONFIG_PACKAGE_kmod-nft-xfrm=y                 # nftables IPsec 匹配支持
CONFIG_PACKAGE_kmod-nvme=n                     # NVMe SSD 驱动（⚠️已取消）
CONFIG_PACKAGE_kmod-ovpn-backports=n           # OpenVPN 内核加速模块（⚠️已取消）
CONFIG_PACKAGE_kmod-sound-core=n               # 声音子系统核心（⚠️ 可取消）
CONFIG_PACKAGE_kmod-tun=y                      # TUN 虚拟网卡（OpenVPN/WireGuard 等需要）
CONFIG_PACKAGE_kmod-usb3=y                     # USB3.0 基础支持
CONFIG_PACKAGE_kmod-usb-audio=n                # USB 声卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-core=y                 # USB 核心支持（必须）
CONFIG_PACKAGE_kmod-usb-dwc3=y                 # DesignWare USB3 控制器驱动（必须）
CONFIG_PACKAGE_kmod-usb-net=n                  # USB 网络设备通用支持（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-asix=n             # ASIX 系列 USB 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-asix-ax88179=n     # AX88179 USB3.0 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-cdc-eem=n          # CDC EEM USB 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-cdc-ether=n        # CDC Ethernet 通用驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-cdc-mbim=n         # MBIM 协议 4G 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=n          # NCM 协议 4G 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-cdc-subset=n       # CDC Subset 驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-huawei-cdc-ncm=n   # 华为 NCM 模式专用驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-ipheth=n           # iPhone USB 共享网络驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=n         # QMI 协议 4G/5G 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan-fibocom=n # 广和通 QMI 支持（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan-quectel=n # 移远 QMI 支持（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-rndis=n            # RNDIS 协议（Android/USB 共享）（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-rtl8150=n          # RTL8150 USB 百兆网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-rtl8152=n          # RTL8152/RTL8153 USB 千兆网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-net-sierrawireless=y   # Sierra Wireless 网卡驱动（⚠️ 可取消）
CONFIG_PACKAGE_kmod-usb-ohci=y                 # USB1.1 OHCI 控制器驱动（可取消）
CONFIG_PACKAGE_kmod-usb-storage=y              # USB 存储设备基础驱动 ✅ 保留
CONFIG_PACKAGE_kmod-usb-storage-extras=y       # 额外 USB 存储设备驱动（特殊 U 盘等）
CONFIG_PACKAGE_kmod-usb-storage-uas=y          # USB Attached SCSI 高速存储协议
CONFIG_PACKAGE_kmod-usb-uhci=y                 # USB1.1 UHCI 控制器驱动（可取消）
CONFIG_PACKAGE_kmod-usb-xhci=y                 # USB3.0 xHCI 控制器驱动（必须）
CONFIG_PACKAGE_kmod-veth=y                     # 虚拟以太网接口（容器/LXC 用）
CONFIG_PACKAGE_kmod-wireguard=y                # WireGuard 内核支持

#============================ 组件调整 ============================
CONFIG_PACKAGE_ddns-scripts-cloudflare=y       # cloudflaeDDNS解析
CONFIG_PACKAGE_ddns-scripts_aliyun=y           # 阿里DDNS解析
CONFIG_PACKAGE_ddns-scripts-dnspod=y           # 腾讯DDNS解析
CONFIG_PACKAGE_htop=y   																
CONFIG_PACKAGE_python3=y 														
CONFIG_PACKAGE_bind-dig=y                      # 域名解析查询工具 
CONFIG_PACKAGE_socat=y                         # 网络工具
CONFIG_PACKAGE_ethtool=y                       # 配置和显示以太网接口参数
CONFIG_PACKAGE_fail2ban=y                      # 防暴力登入破解

CONFIG_PACKAGE_autocore=y                      # 自动核心适配脚本（CPU 调频等）
CONFIG_PACKAGE_automount=y                     # 自动挂载 U 盘等存储设备
CONFIG_PACKAGE_blkid=y                         # 查看块设备 UUID/文件系统类型
CONFIG_PACKAGE_cfdisk=y                        # 图形化磁盘分区工具（curses 界面）
CONFIG_PACKAGE_cgdisk=y                        # GPT 磁盘图形化分区工具
CONFIG_PACKAGE_coremark=y                      # CPU 性能跑分工具（可取消）
CONFIG_PACKAGE_cpufreq=y                       # CPU 频率调节工具
CONFIG_PACKAGE_curl=y                          # curl 命令行下载/上传工具
CONFIG_PACKAGE_dmesg=y                         # 内核日志查看工具
CONFIG_PACKAGE_exfat-fsck=n                    # exFAT 修复工具（⚠️已取消）
CONFIG_PACKAGE_exfat-mkfs=n                    # exFAT 格式化工具（⚠️已取消）
CONFIG_PACKAGE_fdisk=y                         # 传统 MBR 分区工具
CONFIG_PACKAGE_gdisk=y                         # GPT 分区命令行工具
CONFIG_PACKAGE_htop=y                          # 增强型进程查看器
CONFIG_PACKAGE_iperf3=y                        # 网络性能测试工具
CONFIG_PACKAGE_ip-full=y                       # 完整版 ip 命令（替代 busybox 版）
CONFIG_PACKAGE_libimobiledevice=y              # 苹果设备通信库（连接 iPhone）
CONFIG_PACKAGE_libnvme=n                       # NVMe 用户空间库（⚠️已取消）
CONFIG_PACKAGE_lsblk=y                         # 列出块设备信息
CONFIG_PACKAGE_luci-base=y                     # LuCI 网页界面基础
CONFIG_PACKAGE_luci-compat=y                   # LuCI 旧版兼容库
CONFIG_PACKAGE_luci-lib-base=y                 # LuCI 基础依赖库
CONFIG_PACKAGE_luci-lib-ipkg=y                 # LuCI IPK 包管理库
CONFIG_PACKAGE_luci-lua-runtime=y              # LuCI Lua 运行环境
CONFIG_PACKAGE_luci-proto-relay=y              # 协议转发（中继）支持
CONFIG_PACKAGE_mmc-utils=y                     # eMMC 管理工具（查看/分区）
CONFIG_PACKAGE_nand-utils=y                    # NAND Flash 管理工具
CONFIG_PACKAGE_nvme-cli=n                      # NVMe 命令行管理工具（⚠️已取消）
CONFIG_PACKAGE_openssh-keygen=y                # SSH 密钥生成工具
CONFIG_PACKAGE_openssh-sftp-server=y           # SFTP 服务支持
CONFIG_PACKAGE_openssl-util=y                  # OpenSSL 命令行工具
CONFIG_PACKAGE_proto-bonding=y                 # 链路聚合协议脚本
CONFIG_PACKAGE_sfdisk=y                        # 脚本化的磁盘分区工具
CONFIG_PACKAGE_sgdisk=y                        # 脚本化 GPT 分区工具
CONFIG_PACKAGE_smartmontools=n                 # S.M.A.R.T. 硬盘监控（⚠️已取消）
CONFIG_PACKAGE_smartmontools-drivedb=n         # smartmontools 驱动数据库（⚠️已取消）
CONFIG_PACKAGE_usb-modeswitch=y                # USB 模式切换（4G 网卡、无线上网卡）
CONFIG_PACKAGE_usbmuxd=y                       # USB 多路复用守护进程（连接 iPhone）
CONFIG_PACKAGE_usbutils=y                      # USB 设备查看工具（lsusb）
CONFIG_PACKAGE_xz-utils=y                      # xz 压缩/解压工具

